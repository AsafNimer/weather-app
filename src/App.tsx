import "./App.css";
//import { component name} from "components"; // syntax for importing a components i exported:
import { ToggleBtn } from "components";

function App() {
    return (
        <div className="App">
            <ToggleBtn />
        </div>
    );
}

export default App;
