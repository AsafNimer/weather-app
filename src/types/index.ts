export interface resultType {
    name: string;
    country: string;
    state: string;
    lat: number;
    lon: number;
}

// export type addProps = {
//     onCitySelect: (city: resultType) => void;
//     searchResults: [];
// };
